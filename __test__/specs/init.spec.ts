import { expect } from 'chai';

describe('suite', () => {
	it('test #1', () => {
		expect(1).to.equals(1, 'Values does not match');
	});

	it('test #2', () => {
		expect(1).to.equals(1, 'Values does not match');
	});

	it('test #3', () => {
		expect(1).to.equals(1, 'Values does not match');
	});

	it('test #4', () => {
		expect(1).to.equals(1, 'Values does not match');
	});
});
